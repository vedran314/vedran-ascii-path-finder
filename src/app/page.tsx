// Function that formats the map into a string for display.

import { MapList } from "@/features/components/map-list";

// Replaces empty spaces with a dot character.
export default function Home() {
  return <MapList />;
}
